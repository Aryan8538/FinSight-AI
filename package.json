{
  "name": "finsight-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AI-powered financial education, market tracking, and paper portfolio platform for students.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "seed": "npm run seed --prefix server",
    "test": "npm run test --prefix server"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.15.1",
    "morgan": "^1.10.0",
    "zod": "^3.25.42"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
